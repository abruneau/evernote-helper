chai = require 'chai'
sinon = require 'sinon'

{Evernotehelper} = require '../src/evernote-helper'

chai.should()

describe 'evernote-helper', ->
  it 'should do something', ->
    true.should.equal true